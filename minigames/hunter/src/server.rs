use bevy::{app::{App, Plugin, Startup, Update}, ecs::{message::MessageReader, system::ResMut}, state::state::{NextState, States}};

pub struct HunterGameServerPlugin;

impl Plugin for HunterGameServerPlugin {
    fn build(&self, app: &mut App) {

    }
}

